<!DOCTYPE html>
<html>
<head>
  <title>FAM-Truyện Song Ngữ</title>
  <link rel="icon" type="image/gif" href="http://www.freeiconspng.com/uploads/book-stack-icon--icon-search-engine-16.png" />
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="navbar navbar-default navbar-fixed-top navbar-static-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-ex-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#"><span>Brand</span></a>
      </div>
      <div class="collapse navbar-collapse" id="navbar-ex-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li class="active">
            <a href="<%= home_path %>">Trang Chủ<i class="fa fa-border fa-home pull-left"></i></a>
          </li>
          <li>
            <a href="<%= new_path %>">Truyện Mới Cập Nhật</a>
          </li>
          <li>
            <a href="<%= all_path %>">Tất Cả Các Truyện</a>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
            aria-expanded="false">Thể Loại<br><i class="fa fa-caret-down pull-right"></i></a>
            <ul class="dropdown-menu" role="menu">
              <% @theloais.each do |t|%>
              <li>
                <a href="<%= theloai_path(t) %>"><%= t.title%></a>
              </li>
              <%end%>
            </ul>
          </div>
        </div>
      </div>
      <div class="navbar navbar-default navbar-fixed-top navbar-inverse">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-ex-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>
          <div class="collapse navbar-collapse" id="navbar-ex-collapse">
            <ul class="nav navbar-left navbar-nav">
              <li class="active">
                <a href="<%= home_path %>">Trang Chủ<i class="fa fa-border fa-home pull-left"></i></a>
              </li>
              <li>
                <a href="<%= new_path %>">Truyện Mới Cập Nhật</a>
              </li>
              <li>
                <a href="<%= all_path %>">Tất Cả Các Truyện</a>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                aria-expanded="false">Thể Loại<br><i class="fa fa-caret-down pull-right"></i></a>
                <ul class="dropdown-menu" role="menu">
                  <% @theloais.each do |t|%>
                  <li>
                    <a href="<%= theloai_path(t) %>"><%= t.title%></a>
                  </li>
                  <%end%>
                </ul>
              </li>
              <% if current_user %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                aria-expanded="false"><%= current_user.last_name %> <%= current_user.first_name %><br><i class="fa fa-caret-down pull-right"></i></a>
                <ul class="dropdown-menu" role="menu">
                  <li><a href="<%= logout_path%>">Đăng xuất</a>"</li>
                </ul>
              </li>
            </ul>
            <% else %>
          </ul>
          <% end %>
        </ul>
        <%= form_tag(search_path, :method => "get", id: "search-form" ,class: "navbar-form navbar-right text-left") do %>
        <div class="form-group">
          <%= text_field_tag :search, params[:search],class: "form-control" , :placeholder => "Tìm Kiếm"%>
        </div>
        <button type="submit" class="btn btn-default">Tìm Truyện</button>
        <%end%>
        <p class="navbar-right navbar-text">Truyện Song &nbsp;Ngữ
          <br>
        </p>
        <% if current_user %>
        <% else %>
        <a class="btn btn-default navbar-btn navbar-left" data-toggle="modal" href="<%=login_path%>"><i class="fa fa-star fa-fw"></i>Đăng Nhập</a>
        <%end%>
      </div>
    </div>
  </div>

  <%= yield %>

</body>
</html>
